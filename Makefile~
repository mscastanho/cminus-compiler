all: bison flex gcc
	@echo "Done."

bison: tinyParser.y
	bison tinyParser.y

flex: tinyScanner.l
	flex tinyScanner.l

gcc: scanner.c parser.c
	gcc -Wall -o parserty scanner.c parser.c -ly

clean:
	@rm -f *.o *.output scanner.c parser.h parser.c parserty
